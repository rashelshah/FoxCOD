import { createPartialPaymentCheckout } from "./app/services/shopify-partial-payment.server.js";
import 'dotenv/config';

// Mock the unauthenticated module and other dependencies or just run it via ts-node if we can.
