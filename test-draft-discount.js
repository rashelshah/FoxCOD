const { unauthenticated } = require('./app/shopify.server');
// We need to run this as a Remix script or just a plain fetch.
// Actually, I can just write a script with node-fetch if I get the token.
